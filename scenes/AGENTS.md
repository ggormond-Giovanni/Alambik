# scenes/

Scènes Godot minimales : assemblage, collision et script attaché. La logique vit dans `scripts/`.

Une modification de chemin de script doit préserver le `.uid` associé et être vérifiée par `./verifier.sh`.
