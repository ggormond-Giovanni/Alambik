# scripts/monde/

Salle, obstacles et géométrie. Les compositions de rencontres vivent dans `data/vagues.gd`.

Pour tester une ligne libre ou un obstacle, utiliser `Geometrie` plutôt qu'une requête physique dépendante d'une frame.
